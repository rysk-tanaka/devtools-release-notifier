---
title: "Zed Editor - nightly: util: Prevent panic when parsing a malformed shell variable (#63446)"
date: "2026-09-01"
version: "nightly: util: Prevent panic when parsing a malformed shell variable (#63446)"
url: "https://github.com/zed-industries/zed/releases/tag/nightly"
---

# Zed Editor - nightly: util: Prevent panic when parsing a malformed shell variable (#63446)

リリース日: 2026年09月01日

バージョン: nightly: util: Prevent panic when parsing a malformed shell variable (#63446)

リリースURL: [https://github.com/zed-industries/zed/releases/tag/nightly](https://github.com/zed-industries/zed/releases/tag/nightly)

## リリースノート

## 📌 主な変更点
- Windows環境で不正な形式の`${`シェル変数参照を扱った際に発生していたパニック（クラッシュ）を修正
- `to_cmd_variable`と`to_powershell_variable`が閉じ括弧`}`の存在を確認せずに末尾文字を削除していた問題に対応
- `strip_suffix(})`を使用し、変数参照でない場合は入力をそのまま通すように修正
- 修正内容を検証するテストを追加

---

*このドキュメントは自動生成されました*
